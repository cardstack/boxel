name: Deploy boxel-ui with ember

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    concurrency: deploy-ui-${{ inputs.environment }}-${{ github.head_ref || github.run_id }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Init
        uses: ./.github/actions/init

      - name: Set up env
        env:
          INPUT_ENVIRONMENT: ${{ inputs.environment }}
        run: |
          echo "AWS_REGION=us-east-1" >> $GITHUB_ENV
          if [ "$INPUT_ENVIRONMENT" = "staging" ]; then
            echo "AWS_ROLE_ARN=arn:aws:iam::680542703984:role/boxel-ui" >> $GITHUB_ENV
            echo "AWS_S3_BUCKET=cardstack-boxel-ui-staging" >> $GITHUB_ENV
            echo "AWS_CLOUDFRONT_DISTRIBUTION=E3UUDP4PEB5IDV" >> $GITHUB_ENV
          else
            echo "unrecognized environment"
            exit 1;
          fi
      - name: Restore boxel-icons build cache
        id: icons-cache
        uses: actions/cache@d4323d4df104b026a6aa633fdb11d772146be0bf # 4.2.2
        with:
          path: |
            packages/boxel-icons/dist
            packages/boxel-icons/declarations
          key: boxel-icons-${{ hashFiles('packages/boxel-icons/src/**', 'packages/boxel-icons/package.json', 'packages/boxel-icons/rollup.config.mjs', 'packages/boxel-icons/tsconfig.json', 'packages/boxel-icons/babel.config.json') }}
      - name: Build boxel-icons and boxel-ui
        run: mise run build:ui
        env:
          SKIP_ICONS_BUILD: ${{ steps.icons-cache.outputs.cache-hit }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ececac1a45f3b08a01d2dd070d28d111c5fe6722 # 4.1.0
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Deploy
        run: pnpm deploy:boxel-ui ${{ inputs.environment }} --verbose

      - name: Send notification to Discord
        uses: cardstack/gh-actions/discord-notification-deploy@main
        with:
          app: "boxel-ui"
          status: ${{ job.status }}
          environment: ${{ inputs.environment }}
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
