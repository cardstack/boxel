name: Diff Skills

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  diffs-skills:
    uses: cardstack/gh-actions/.github/workflows/diff-skills.yml@skills-matrix-cs-8727
    secrets: inherit
