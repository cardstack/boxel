name: Diff Skills

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  diff-skills:
    uses: cardstack/gh-actions/.github/workflows/diff-skills.yml@main
    secrets: inherit
