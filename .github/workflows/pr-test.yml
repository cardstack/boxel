name: CI

on:
  pull_request:

env:
  VOLTA_FEATURE_PNPM: 1

permissions:
  contents: read
  id-token: write

jobs:
  deploy-realm-server:
    uses: ./.github/workflows/deploy-realm-server.yml
    secrets: inherit
    with:
      environment: staging
