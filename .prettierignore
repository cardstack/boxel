# Committed bench-input artifacts — generated by `pnpm bench:amd:prep`,
# must match what the loader actually receives. Do not reformat.
bench-fixtures/

# pnpm owns this file's formatting; prettier would fight pnpm's serializer
# and rewrite the whole file on every commit that stages it.
pnpm-lock.yaml
