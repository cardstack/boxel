{
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/cardstack/boxel-motion"
  },
  "workspaces": [
    "addon",
    "demo-app",
    "test-app"
  ],
  "volta": {
    "node": "14.18.3"
  },
  "scripts": {
    "lint": "pnpm run --if-present -r lint",
    "lint:fix": "pnpm run --if-present -r lint:fix"
  },
  "pnpm": {
    "overrides": {
      "@types/eslint": "8.4.1"
    },
    "peerDependencyRules": {
      "allowedVersions": {
        "mustache": "3"
      }
    }
  },
  "devDependencies": {
    "eslint-plugin-import": "^2.26.0"
  }
}
