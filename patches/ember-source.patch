diff --git a/dist/ember.debug.js b/dist/ember.debug.js
--- a/dist/ember.debug.js
+++ b/dist/ember.debug.js
@@ -6203,7 +6203,14 @@
             return positional.length;
           }
           function meta$1(layout) {
-            let [, locals, upvars, lexicalSymbols] = layout.block;
+            let block = layout.block;
+            let locals, upvars, lexicalSymbols;
+            if (block.length === 4 && typeof block[2] === 'boolean') {
+              [, locals, , upvars] = block;
+              lexicalSymbols = undefined;
+            } else {
+              [, locals, upvars, lexicalSymbols] = block;
+            }
             return {
               symbols: {
                 locals: locals,
diff --git a/dist/ember.prod.js b/dist/ember.prod.js
--- a/dist/ember.prod.js
+++ b/dist/ember.prod.js
@@ -5126,7 +5126,14 @@
             return positional.length;
           }
           function meta$1(layout) {
-            let [, locals, upvars, lexicalSymbols] = layout.block;
+            let block = layout.block;
+            let locals, upvars, lexicalSymbols;
+            if (block.length === 4 && typeof block[2] === 'boolean') {
+              [, locals, , upvars] = block;
+              lexicalSymbols = undefined;
+            } else {
+              [, locals, upvars, lexicalSymbols] = block;
+            }
             return {
               symbols: {
                 locals: locals,
diff --git a/dist/ember-template-compiler.js b/dist/ember-template-compiler.js
--- a/dist/ember-template-compiler.js
+++ b/dist/ember-template-compiler.js
@@ -16479,7 +16479,6 @@
           if (typeof module === 'object' && module.exports) {
             module.exports = emberTemplateCompilerIndex;
           }
-          console.log('\n\n--------\n+ Your app is using the legacy ember-template-compiler.js AMD bundle. This will be removed in ember-source 7.0. See https://deprecations.emberjs.com/id/using-amd-bundles for more details.\n--------');

 })((() => {
       try {
diff --git a/dist/packages/@glimmer/opcode-compiler/index.js b/dist/packages/@glimmer/opcode-compiler/index.js
--- a/dist/packages/@glimmer/opcode-compiler/index.js
+++ b/dist/packages/@glimmer/opcode-compiler/index.js
@@ -615,7 +615,16 @@ function CompilePositional(op, positional) {
   return positional.length;
 }
 function meta(layout) {
-  let [, locals, upvars, lexicalSymbols] = layout.block;
+  let block = layout.block;
+  let locals, upvars, lexicalSymbols;
+  if (block.length === 4 && typeof block[2] === 'boolean') {
+    // Ember 5 wire format: [statements, symbols, hasEval, upvars]
+    [, locals, , upvars] = block;
+    lexicalSymbols = undefined;
+  } else {
+    // Ember 6 wire format: [statements, locals, upvars, lexicalSymbols]
+    [, locals, upvars, lexicalSymbols] = block;
+  }
   return {
     symbols: {
       locals: locals,
