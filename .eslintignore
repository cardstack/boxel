# Committed bench-input artifacts — generated by `pnpm bench:amd:prep`,
# must match what the loader actually receives. Do not autofix.
bench-fixtures/

# Generated BXL runtime bundled into the synthetic clinical review realm.
# Authored realm sources are linted; this copied build artifact is not.
packages/bxl-clinical-mutation-realm/bxl/index.ts
