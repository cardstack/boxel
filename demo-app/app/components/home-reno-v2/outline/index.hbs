<HomeRenoV2::Card
  @state={{@state}}
  @minimize={{fn @minimize @card}}
  @maximize={{fn @maximize @card}}
  @expand={{fn @expand @card}}
>
  {{#if (eq @state "MIN")}}
      Outline
  {{else if (eq @state "MAX")}}
      <ul local-class="list">
        <li>
          <h2 local-class="list-heading">
            Duck selection
          </h2>
          <ol local-class="list indented">
            <li>
              What duck?
            </li>
            <li>
              This duck
            </li>
            <li>
              Get it!
            </li>
          </ol>
        </li>
        <li>
          <h2 local-class="list-heading">
            Still duck selection
          </h2>
          <ol local-class="list indented">
            <li>
              What duck?
            </li>
            <li>
              This duck
            </li>
            <li>
              Get it!
            </li>
          </ol>
        </li>
      </ul>
      <ul local-class="suggestion-list">
        {{#each @suggestions as |suggestion|}}
          <li>
            {{#if (eq suggestion.state "MAX")}}
              <HomeRenoV2::Placeholder/>
            {{else}}
              {{component
                (concat "home-reno-v2/" suggestion.type)
                  card=suggestion
                  model=suggestion.model
                  state=suggestion.state
                  suggestions=suggestion.suggestions
                  canUpdateState=suggestion.canUpdateState
                  minimize=@minimize
                  maximize=@maximize
                  expand=@expand
              }}
            {{/if}}
          </li>
        {{/each}}
      </ul>
  {{else if (eq @state "EXPANDED")}}
      Outline
      <div>
        A list of duck-related questions
      </div>
  {{/if}}
</HomeRenoV2::Card>