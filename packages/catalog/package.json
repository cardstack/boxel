{
  "name": "@cardstack/catalog",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "catalog:setup": "./scripts/setup.sh",
    "catalog:update": "pnpm catalog:setup && cd contents && git pull",
    "catalog:reset": "rm -rf contents && pnpm catalog:setup"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
