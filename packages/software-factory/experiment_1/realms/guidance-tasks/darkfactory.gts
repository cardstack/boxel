// ═══ [EDIT TRACKING: ON] Mark all changes with ⁿ ═══
// ¹ Barrel re-export — instances reference this module; schema + UI live in separate files
export { AgentProfile, KnowledgeArticle, Ticket, Project, DarkFactory } from './darkfactory-ui'; // ² re-export all card types (with templates) from UI file
// touched for re-index
