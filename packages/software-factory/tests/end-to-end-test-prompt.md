Pretend that you are a human and execute the software factory CLI command as outlined in the README.md. you can use the local boxel-cli profile for the account user/password There is a local server running. use the --debug parameter to be able to debug any tool call errors or prompt weirdness. make sure that each time you do an end to end test you use a different target realm. after the factory is complete use a chrome browser to login to the boxel app as user/password and use the mouse to navigate to the target realm and open the different artifacts in this realm and confirm they line up with what the README.md describes is expected.
