{
  "name": "@cardstack/catalog-new",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "catalog-new:setup": "[ -d contents ] || (git clone git@github.com:cardstack/boxel-catalog.git contents || git clone https://github.com/cardstack/boxel-catalog.git contents)",
    "catalog-new:update": "pnpm catalog-new:setup && cd contents && git pull",
    "catalog-new:reset": "rm -rf contents && pnpm catalog-new:setup"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
