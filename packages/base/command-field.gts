// Pre-rename module URL for `./tool-field`. Imports and indexed dependencies
// authored before the command → tool rename resolve here; new code imports
// `./tool-field` directly.
export * from './tool-field';
