# A credential file dropped here must never be committed.
*.csv

# A real workload file transcribes the card types of the realm under test,
# which generally belong to someone else. Only the example belongs in the repo.
workload*.json
!workload.example.json
