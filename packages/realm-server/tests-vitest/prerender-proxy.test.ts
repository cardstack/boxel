// AUTO-GENERATED by scripts/codemods/qunit-to-vitest.ts. Run codemod again instead of editing by hand.
import { describe, it, expect } from 'vitest';
import Koa from 'koa';
import Router from '@koa/router';
import supertest from 'supertest';
import type { DBAdapter, Prerenderer } from '@cardstack/runtime-common';
import type { RenderRouteOptions } from '@cardstack/runtime-common';
import handlePrerenderProxy from '../handlers/handle-prerender-proxy';
import { jwtMiddleware } from '../middleware';
import { createJWT } from '../utils/jwt';
import { realmSecretSeed } from './helpers';
import { buildCreatePrerenderAuth } from '../prerender/auth';
import { verifyJWT } from '../jwt';
describe('prerender-proxy-test.ts', function () {
  describe('prerender proxy', function () {
    let createPrerenderAuth = buildCreatePrerenderAuth(realmSecretSeed);
    function makeDbAdapter(rows: any[]): DBAdapter {
      return {
        kind: 'pg',
        isClosed: false,
        async execute() {
          return rows;
        },
        async close() {},
        async getColumnNames() {
          return [];
        },
      };
    }
    function makePrerenderer() {
      let renderCalls: Array<{
        kind: 'card' | 'module' | 'file-extract' | 'file-render' | 'command';
        args: {
          affinityType?: 'realm' | 'user';
          affinityValue?: string;
          realm?: string;
          userId?: string;
          url?: string;
          auth: string;
          command?: string;
          commandInput?: Record<string, unknown> | null;
          renderOptions?: RenderRouteOptions;
        };
      }> = [];
      let prerenderer: Prerenderer = {
        async prerenderCard(args) {
          renderCalls.push({ kind: 'card', args });
          return {
            serialized: null,
            searchDoc: { url: args.url, cardTitle: 'through proxy' },
            displayNames: ['Proxy Card'],
            deps: [],
            types: [],
            isolatedHTML: `<div>${args.url}</div>`,
            headHTML: null,
            atomHTML: null,
            embeddedHTML: {},
            fittedHTML: {},
            iconHTML: null,
          };
        },
        async prerenderModule(args) {
          renderCalls.push({ kind: 'module', args });
          return {
            id: args.url,
            status: 'ready',
            nonce: 'nonce',
            isShimmed: false,
            lastModified: Date.now(),
            createdAt: Date.now(),
            deps: [],
            definitions: {},
          };
        },
        async prerenderFileExtract(args) {
          renderCalls.push({ kind: 'file-extract', args });
          return {
            id: args.url,
            nonce: 'nonce',
            status: 'ready',
            searchDoc: { url: args.url, title: 'through proxy' },
            deps: [],
          };
        },
        async prerenderFileRender(args) {
          renderCalls.push({ kind: 'file-render', args });
          return {
            isolatedHTML: null,
            headHTML: null,
            atomHTML: null,
            embeddedHTML: null,
            fittedHTML: null,
            iconHTML: null,
          };
        },
        async runCommand(args) {
          renderCalls.push({ kind: 'command', args });
          return {
            status: 'ready',
            cardResultString: null,
          };
        },
      };
      return { prerenderer, renderCalls };
    }
    it('proxies prerender requests to the configured prerenderer', async function () {
      let { prerenderer, renderCalls } = makePrerenderer();
      let dbAdapter = makeDbAdapter([
        {
          username: '@someone:localhost',
          read: true,
          write: true,
          realm_owner: false,
        },
      ]);
      let app = new Koa();
      let router = new Router();
      router.post(
        '/_prerender-card',
        jwtMiddleware(realmSecretSeed),
        handlePrerenderProxy({
          kind: 'card',
          prerenderer,
          dbAdapter,
          createPrerenderAuth,
        }),
      );
      app.use(router.routes());
      let token = createJWT(
        { user: '@someone:localhost', sessionRoom: '!room:localhost' },
        realmSecretSeed,
      );
      let cardURL = 'http://example/card';
      let realm = 'http://example/';
      let payload = {
        data: {
          attributes: { realm, url: cardURL },
        },
      };
      let response = await supertest(app.callback())
        .post('/_prerender-card')
        .set('Authorization', `Bearer ${token}`)
        .send(payload)
        .expect(201);
      expect(response.body).toEqual({
        data: {
          type: 'prerender-result',
          id: cardURL,
          attributes: {
            serialized: null,
            searchDoc: { url: cardURL, cardTitle: 'through proxy' },
            displayNames: ['Proxy Card'],
            deps: [],
            types: [],
            isolatedHTML: `<div>${cardURL}</div>`,
            headHTML: null,
            atomHTML: null,
            embeddedHTML: {},
            fittedHTML: {},
            iconHTML: null,
          },
        },
      });
      expect(renderCalls.length).toEqual(1);
      expect(renderCalls[0]?.kind).toBe('card');
      expect(renderCalls[0]?.args).toEqual({
        affinityType: 'realm',
        affinityValue: realm,
        realm,
        url: cardURL,
        auth: renderCalls[0]?.args.auth,
        renderOptions: undefined,
      });
      let sessions = JSON.parse(renderCalls[0]!.args.auth);
      let tokenClaims = verifyJWT(sessions[realm], realmSecretSeed);
      expect(tokenClaims.user).toBe('@someone:localhost');
      expect(tokenClaims.permissions).toEqual(['read', 'write']);
      expect(tokenClaims.realm).toBe(realm);
    });
    it('returns an error when no upstream is configured', async function () {
      let app = new Koa();
      let router = new Router();
      router.post(
        '/_prerender-card',
        jwtMiddleware(realmSecretSeed),
        handlePrerenderProxy({
          kind: 'card',
          prerenderer: undefined,
          dbAdapter: makeDbAdapter([]),
          createPrerenderAuth,
        }),
      );
      app.use(router.routes());
      let token = createJWT(
        { user: '@someone:localhost', sessionRoom: '!room:localhost' },
        realmSecretSeed,
      );
      let res = await supertest(app.callback())
        .post('/_prerender-card')
        .set('Authorization', `Bearer ${token}`)
        .send({ data: { attributes: {} } })
        .expect(500);
      expect(
        res.text.includes('Prerender proxy is not configured'),
      ).toBeTruthy();
    });
    it('returns unauthorized when no token is provided', async function () {
      let { prerenderer } = makePrerenderer();
      let app = new Koa();
      let router = new Router();
      router.post(
        '/_prerender-card',
        jwtMiddleware(realmSecretSeed),
        handlePrerenderProxy({
          kind: 'card',
          prerenderer,
          dbAdapter: makeDbAdapter([]),
          createPrerenderAuth,
        }),
      );
      app.use(router.routes());
      let res = await supertest(app.callback())
        .post('/_prerender-card')
        .send({
          data: {
            attributes: {
              realm: 'http://localhost:4201/base/',
              url: 'http://localhost:4201/base/some-card',
            },
          },
        })
        .expect(401);
      expect(res.body.errors).toEqual(['Missing Authorization header']);
    });
    it('returns forbidden when user has no realm permissions', async function () {
      let { prerenderer, renderCalls } = makePrerenderer();
      let app = new Koa();
      let router = new Router();
      router.post(
        '/_prerender-card',
        jwtMiddleware(realmSecretSeed),
        handlePrerenderProxy({
          kind: 'card',
          prerenderer,
          dbAdapter: makeDbAdapter([]), // no permissions
          createPrerenderAuth,
        }),
      );
      app.use(router.routes());
      let token = createJWT(
        { user: '@someone:localhost', sessionRoom: '!room:localhost' },
        realmSecretSeed,
      );
      let res = await supertest(app.callback())
        .post('/_prerender-card')
        .set('Authorization', `Bearer ${token}`)
        .send({
          data: {
            attributes: {
              realm: 'http://localhost:4201/base/',
              url: 'http://localhost:4201/base/some-card',
            },
          },
        });
      expect(res.status).toBe(403);
      expect(renderCalls).toEqual([]);
    });
    it('proxies to prerender server card and module endpoints', async function () {
      let { prerenderer, renderCalls } = makePrerenderer();
      let realm = 'http://example.test/';
      let dbAdapter = makeDbAdapter([
        {
          username: '@someone:localhost',
          read: true,
          write: true,
          realm_owner: false,
        },
      ]);
      let app = new Koa();
      let router = new Router();
      router.post(
        '/_prerender-card',
        jwtMiddleware(realmSecretSeed),
        handlePrerenderProxy({
          kind: 'card',
          prerenderer,
          dbAdapter,
          createPrerenderAuth,
        }),
      );
      router.post(
        '/_prerender-module',
        jwtMiddleware(realmSecretSeed),
        handlePrerenderProxy({
          kind: 'module',
          prerenderer,
          dbAdapter,
          createPrerenderAuth,
        }),
      );
      app.use(router.routes());
      let token = createJWT(
        { user: '@someone:localhost', sessionRoom: '!room:localhost' },
        realmSecretSeed,
      );
      let cardUrl = `${realm}card`;
      let cardResponse = await supertest(app.callback())
        .post('/_prerender-card')
        .set('Authorization', `Bearer ${token}`)
        .send({
          data: { attributes: { realm, url: cardUrl } },
        })
        .expect(201);
      expect(cardResponse.body.data.type).toBe('prerender-result');
      expect(cardResponse.body.data.id).toBe(cardUrl);
      expect(cardResponse.body.data.attributes.displayNames).toEqual([
        'Proxy Card',
      ]);
      let moduleUrl = `${realm}module.gts`;
      let moduleResponse = await supertest(app.callback())
        .post('/_prerender-module')
        .set('Authorization', `Bearer ${token}`)
        .send({
          data: { attributes: { realm, url: moduleUrl } },
        })
        .expect(201);
      expect(moduleResponse.body.data.type).toBe('prerender-module-result');
      expect(moduleResponse.body.data.id).toBe(moduleUrl);
      expect(moduleResponse.body.data.attributes.status).toBe('ready');
      expect(
        renderCalls.map(({ kind, args }) => {
          let sessions = JSON.parse(args.auth);
          let claims = verifyJWT(sessions[realm], realmSecretSeed);
          return {
            kind,
            realm: args.realm,
            url: args.url,
            permissions: { [claims.realm]: claims.permissions },
            userId: claims.user,
          };
        }),
      ).toEqual([
        {
          kind: 'card',
          realm,
          url: cardUrl,
          permissions: { [realm]: ['read', 'write'] },
          userId: '@someone:localhost',
        },
        {
          kind: 'module',
          realm,
          url: moduleUrl,
          permissions: { [realm]: ['read', 'write'] },
          userId: '@someone:localhost',
        },
      ]);
    });
  });
});
