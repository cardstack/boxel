// AUTO-GENERATED by scripts/codemods/qunit-to-vitest.ts. Run codemod again instead of editing by hand.
import { describe, test, expect } from "vitest";
import { runSharedTest } from '@cardstack/runtime-common/helpers';
import runCommandTaskTests from '@cardstack/runtime-common/tests/run-command-task-shared-tests';
describe("run-command-task-test.ts", function () {
    describe('run-command task', function () {
        test('returns error when runAs has no realm permissions', async function () {
            await runSharedTest(runCommandTaskTests, assert, {});
        });
        test('returns error when command specifier is invalid', async function () {
            await runSharedTest(runCommandTaskTests, assert, {});
        });
        test('normalizes legacy /commands URL and defaults export name', async function () {
            await runSharedTest(runCommandTaskTests, assert, {});
        });
        test('passes scoped command through unchanged', async function () {
            await runSharedTest(runCommandTaskTests, assert, {});
        });
    });
});
