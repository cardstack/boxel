// AUTO-GENERATED by scripts/codemods/qunit-to-vitest.ts. Run codemod again instead of editing by hand.
import { describe, test, expect, beforeAll, beforeEach, afterAll, afterEach } from "vitest";
import supertest from 'supertest';
import type { Test, SuperTest } from 'supertest';
import { join } from 'path';
import { dirSync } from 'tmp';
import type { LooseSingleCardDocument, QueuePublisher, QueueRunner, Realm, } from '@cardstack/runtime-common';
import { baseCardRef, buildQueryParamValue } from '@cardstack/runtime-common';
import type { Query } from '@cardstack/runtime-common/query';
import type { PgAdapter } from '@cardstack/postgres';
import { resetCatalogRealms } from '../../handlers/handle-fetch-catalog-realms';
import { closeServer, createVirtualNetwork, setupDB, matrixURL, realmSecretSeed, runTestRealmServerWithRealms, } from '../helpers';
import { createJWT as createRealmServerJWT } from '../../utils/jwt';
import type { Server } from 'http';
describe("server-endpoints/search-prerendered-test.ts", function () {
    const _hooks = {
        before: beforeAll,
        after: afterAll,
        beforeEach,
        afterEach
    };
    describe('Realm Server Endpoints | /_federated-search-prerendered', function () {
        const hooks = {
            before: beforeAll,
            after: afterAll,
            beforeEach,
            afterEach
        };
        let testRealm: Realm;
        let secondaryRealm: Realm;
        let request: SuperTest<Test>;
        let dbAdapter: PgAdapter;
        let testRealmHttpServer: Server;
        let ownerUserId = '@mango:localhost';
        let realmFileSystem: Record<string, LooseSingleCardDocument> = {
            'test-card.json': {
                data: {
                    type: 'card',
                    attributes: {
                        cardInfo: {
                            name: 'Shared Card',
                        },
                    },
                    meta: {
                        adoptsFrom: {
                            module: 'https://cardstack.com/base/card-api',
                            name: 'CardDef',
                        },
                    },
                },
            },
            'other-card.json': {
                data: {
                    type: 'card',
                    attributes: {
                        cardInfo: {
                            name: 'Other Card',
                        },
                    },
                    meta: {
                        adoptsFrom: {
                            module: 'https://cardstack.com/base/card-api',
                            name: 'CardDef',
                        },
                    },
                },
            },
        };
        async function startSearchRealmServer({ dbAdapter, publisher, runner, }: {
            dbAdapter: PgAdapter;
            publisher: QueuePublisher;
            runner: QueueRunner;
        }) {
            let virtualNetwork = createVirtualNetwork();
            let dir = dirSync();
            let testRealmURL = new URL('http://127.0.0.1:4444/test/');
            let secondaryRealmURL = new URL('http://127.0.0.1:4444/secondary/');
            let result = await runTestRealmServerWithRealms({
                virtualNetwork,
                realmsRootPath: join(dir.name, 'realm_server_1'),
                realms: [
                    {
                        realmURL: testRealmURL,
                        fileSystem: realmFileSystem,
                        permissions: {
                            [ownerUserId]: ['read', 'write', 'realm-owner'],
                        },
                    },
                    {
                        realmURL: secondaryRealmURL,
                        fileSystem: realmFileSystem,
                        permissions: {
                            [ownerUserId]: ['read', 'write', 'realm-owner'],
                        },
                    },
                ],
                dbAdapter,
                publisher,
                runner,
                matrixURL,
            });
            testRealmHttpServer = result.testRealmHttpServer;
            request = supertest(result.testRealmHttpServer);
            testRealm = result.realms.find((realm) => realm.url === testRealmURL.href)!;
            secondaryRealm = result.realms.find((realm) => realm.url === secondaryRealmURL.href)!;
        }
        async function stopSearchRealmServer() {
            testRealm.unsubscribe();
            secondaryRealm.unsubscribe();
            await closeServer(testRealmHttpServer);
            resetCatalogRealms();
        }
        setupDB(hooks, {
            beforeEach: async (_dbAdapter, publisher, runner) => {
                dbAdapter = _dbAdapter;
                await startSearchRealmServer({
                    dbAdapter,
                    publisher,
                    runner,
                });
            },
            afterEach: async () => {
                await stopSearchRealmServer();
            },
        });
        test('QUERY /_federated-search-prerendered federates results across realms', async function () {
            let realmServerToken = createRealmServerJWT({ user: ownerUserId, sessionRoom: 'session-room-test' }, realmSecretSeed);
            let query: Query = {
                filter: {
                    on: baseCardRef,
                    eq: {
                        cardTitle: 'Shared Card',
                    },
                },
            };
            let searchURL = new URL('/_federated-search-prerendered', testRealm.url);
            let searchResponse = await request
                .post(`${searchURL.pathname}${searchURL.search}`)
                .set('Accept', 'application/vnd.card+json')
                .set('Content-Type', 'application/json')
                .set('X-HTTP-Method-Override', 'QUERY')
                .set('Authorization', `Bearer ${realmServerToken}`)
                .send({
                ...query,
                realms: [testRealm.url, secondaryRealm.url],
                prerenderedHtmlFormat: 'embedded',
            });
            expect(searchResponse.status).toBe(200);
            let results = searchResponse.body;
            expect(results.data.length).toBe(2);
            expect(results.meta.page.total).toBe(2);
            let ids: string[] = results.data.map((entry: {
                id: string;
            }) => entry.id);
            expect(ids[0]?.startsWith(testRealm.url)).toBeTruthy();
            expect(ids[1]?.startsWith(secondaryRealm.url)).toBeTruthy();
            expect(ids.every((id) => id.includes('test-card'))).toBe(true);
        });
        test('GET /_federated-search-prerendered returns 400 for unsupported method', async function () {
            let realmServerToken = createRealmServerJWT({ user: ownerUserId, sessionRoom: 'session-room-test' }, realmSecretSeed);
            let query: Query = {
                filter: {
                    on: baseCardRef,
                    eq: {
                        cardTitle: 'Shared Card',
                    },
                },
            };
            let searchURL = new URL('/_federated-search-prerendered', testRealm.url);
            searchURL.searchParams.append('realms', testRealm.url);
            searchURL.searchParams.set('query', buildQueryParamValue(query));
            searchURL.searchParams.set('prerenderedHtmlFormat', 'embedded');
            let response = await request
                .get(`${searchURL.pathname}${searchURL.search}`)
                .set('Accept', 'application/vnd.card+json')
                .set('Authorization', `Bearer ${realmServerToken}`);
            expect(response.status).toBe(400);
            expect(response.body.errors?.[0]?.includes('method must be QUERY')).toBeTruthy();
        });
        test('QUERY /_federated-search-prerendered returns 403 when user lacks read access', async function () {
            let realmServerToken = createRealmServerJWT({ user: '@rando:localhost', sessionRoom: 'session-room-test' }, realmSecretSeed);
            let query: Query = {
                filter: {
                    on: baseCardRef,
                    eq: {
                        cardTitle: 'Test Card',
                    },
                },
            };
            let searchURL = new URL('/_federated-search-prerendered', testRealm.url);
            let response = await request
                .post(`${searchURL.pathname}${searchURL.search}`)
                .set('Accept', 'application/vnd.card+json')
                .set('Content-Type', 'application/json')
                .set('X-HTTP-Method-Override', 'QUERY')
                .set('Authorization', `Bearer ${realmServerToken}`)
                .send({
                ...query,
                realms: [testRealm.url],
                prerenderedHtmlFormat: 'embedded',
            });
            expect(response.status).toBe(403);
            expect(response.body.errors?.[0]?.includes(testRealm.url)).toBeTruthy();
        });
        test('QUERY /_federated-search-prerendered returns 401 when unauthenticated user requests non-public realm', async function () {
            let query: Query = {
                filter: {
                    on: baseCardRef,
                    eq: {
                        cardTitle: 'Test Card',
                    },
                },
            };
            let searchURL = new URL('/_federated-search-prerendered', testRealm.url);
            let response = await request
                .post(`${searchURL.pathname}${searchURL.search}`)
                .set('Accept', 'application/vnd.card+json')
                .set('Content-Type', 'application/json')
                .set('X-HTTP-Method-Override', 'QUERY')
                .send({
                ...query,
                realms: [testRealm.url],
                prerenderedHtmlFormat: 'embedded',
            });
            expect(response.status).toBe(401);
            expect(response.body.errors?.[0]?.includes(testRealm.url)).toBeTruthy();
        });
        test('QUERY /_federated-search-prerendered returns 400 for invalid query', async function () {
            let realmServerToken = createRealmServerJWT({ user: ownerUserId, sessionRoom: 'session-room-test' }, realmSecretSeed);
            let searchURL = new URL('/_federated-search-prerendered', testRealm.url);
            let response = await request
                .post(`${searchURL.pathname}${searchURL.search}`)
                .set('Accept', 'application/vnd.card+json')
                .set('Content-Type', 'application/json')
                .set('X-HTTP-Method-Override', 'QUERY')
                .set('Authorization', `Bearer ${realmServerToken}`)
                .send({
                realms: [testRealm.url],
                invalid: 'query structure',
                prerenderedHtmlFormat: 'embedded',
            });
            expect(response.status).toBe(400);
        });
        test('QUERY /_federated-search-prerendered returns 400 when realms param is missing', async function () {
            let query: Query = {
                filter: {
                    on: baseCardRef,
                    eq: {
                        cardTitle: 'Test Card',
                    },
                },
            };
            let response = await request
                .post('/_federated-search-prerendered')
                .set('Accept', 'application/vnd.card+json')
                .set('Content-Type', 'application/json')
                .set('X-HTTP-Method-Override', 'QUERY')
                .send({ ...query, prerenderedHtmlFormat: 'embedded' });
            expect(response.status).toBe(400);
            expect(response.body.errors?.[0]?.includes('realms must be supplied in request body')).toBeTruthy();
        });
    });
});
