// AUTO-GENERATED by scripts/codemods/qunit-to-vitest.ts. Run codemod again instead of editing by hand.
import { describe, it, expect, beforeAll, beforeEach, afterAll, afterEach } from "vitest";
import { createJWT as createRealmServerJWT } from '../../utils/jwt';
import { realmSecretSeed, insertUser } from '../helpers';
import { param, query, uuidv4 } from '@cardstack/runtime-common';
import { setupServerEndpointsTest } from './helpers';
describe("server-endpoints/webhook-commands-test.ts", function () {
    describe('Webhook Command Endpoints', function () {
        const hooks = {
            before: beforeAll,
            after: afterAll,
            beforeEach,
            afterEach
        };
        let context = setupServerEndpointsTest(hooks);
        it('requires auth to add webhook command', async function () {
            let response = await context.request.post('/_webhook-commands').send({});
            expect(response.status).toBe(401);
        });
        it('requires auth to list webhook commands', async function () {
            let response = await context.request.get('/_webhook-commands');
            expect(response.status).toBe(401);
        });
        it('requires auth to delete webhook command', async function () {
            let response = await context.request.delete('/_webhook-commands').send({
                data: {
                    type: 'webhook-command',
                    id: uuidv4(),
                },
            });
            expect(response.status).toBe(401);
        });
        it('can add webhook command for own webhook', async function () {
            let matrixUserId = '@user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            let incomingWebhookId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(incomingWebhookId),
                `,`,
                param(matrixUserId),
                `,`,
                param('whk_test1'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let response = await context.request
                .post('/_webhook-commands')
                .set('Accept', 'application/vnd.api+json')
                .set('Content-Type', 'application/vnd.api+json')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`)
                .send({
                data: {
                    type: 'webhook-command',
                    attributes: {
                        incomingWebhookId,
                        command: 'https://example.com/webhook-handler',
                        filter: {
                            eventType: 'pull_request',
                        },
                    },
                },
            });
            expect(response.status).toBe(201);
            expect(response.body.data.id).toBeTruthy();
            expect(response.body.data.attributes.incomingWebhookId).toBe(incomingWebhookId);
            expect(response.body.data.attributes.command).toBe('https://example.com/webhook-handler');
            expect(response.body.data.attributes.filter).toEqual({ eventType: 'pull_request' });
            expect(response.body.data.attributes.createdAt).toBeTruthy();
            expect(response.body.data.attributes.updatedAt).toBeTruthy();
            let rows = await context.dbAdapter.execute(`SELECT id, incoming_webhook_id, command, command_filter, created_at, updated_at FROM webhook_commands`);
            expect(rows.length).toBe(1);
            expect(rows[0].incoming_webhook_id).toBe(incomingWebhookId);
            expect(rows[0].command).toBe('https://example.com/webhook-handler');
        });
        it('can add webhook command with null filter', async function () {
            let matrixUserId = '@user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            let incomingWebhookId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(incomingWebhookId),
                `,`,
                param(matrixUserId),
                `,`,
                param('whk_test2'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let response = await context.request
                .post('/_webhook-commands')
                .set('Accept', 'application/vnd.api+json')
                .set('Content-Type', 'application/vnd.api+json')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`)
                .send({
                data: {
                    type: 'webhook-command',
                    attributes: {
                        incomingWebhookId,
                        command: 'https://example.com/webhook-handler',
                    },
                },
            });
            expect(response.status).toBe(201);
            expect(response.body.data.attributes.filter).toBe(null);
        });
        it('rejects webhook command for another user webhook', async function () {
            let matrixUserId = '@user:localhost';
            let otherMatrixUserId = '@other-user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            await insertUser(context.dbAdapter, otherMatrixUserId, 'cus_124', 'other@example.com');
            let incomingWebhookId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(incomingWebhookId),
                `,`,
                param(otherMatrixUserId),
                `,`,
                param('whk_other1'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let response = await context.request
                .post('/_webhook-commands')
                .set('Accept', 'application/vnd.api+json')
                .set('Content-Type', 'application/vnd.api+json')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`)
                .send({
                data: {
                    type: 'webhook-command',
                    attributes: {
                        incomingWebhookId,
                        command: 'https://example.com/webhook-handler',
                        filter: { eventType: 'push' },
                    },
                },
            });
            expect(response.status).toBe(403);
        });
        it('rejects webhook command when webhook is not found', async function () {
            let matrixUserId = '@user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            let response = await context.request
                .post('/_webhook-commands')
                .set('Accept', 'application/vnd.api+json')
                .set('Content-Type', 'application/vnd.api+json')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`)
                .send({
                data: {
                    type: 'webhook-command',
                    attributes: {
                        incomingWebhookId: uuidv4(),
                        command: 'https://example.com/webhook-handler',
                        filter: { eventType: 'push' },
                    },
                },
            });
            expect(response.status).toBe(404);
        });
        it('rejects webhook command with invalid incomingWebhookId', async function () {
            let matrixUserId = '@user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            let response = await context.request
                .post('/_webhook-commands')
                .set('Accept', 'application/vnd.api+json')
                .set('Content-Type', 'application/vnd.api+json')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`)
                .send({
                data: {
                    type: 'webhook-command',
                    attributes: {
                        incomingWebhookId: 'not-a-uuid',
                        command: 'https://example.com/webhook-handler',
                        filter: { eventType: 'push' },
                    },
                },
            });
            expect(response.status).toBe(400);
        });
        it('rejects invalid command URL', async function () {
            let matrixUserId = '@user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            let incomingWebhookId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(incomingWebhookId),
                `,`,
                param(matrixUserId),
                `,`,
                param('whk_test3'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let response = await context.request
                .post('/_webhook-commands')
                .set('Accept', 'application/vnd.api+json')
                .set('Content-Type', 'application/vnd.api+json')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`)
                .send({
                data: {
                    type: 'webhook-command',
                    attributes: {
                        incomingWebhookId,
                        command: '   ',
                        filter: { eventType: 'push' },
                    },
                },
            });
            expect(response.status).toBe(400);
        });
        it('lists webhook commands for authenticated user', async function () {
            let matrixUserId = '@user:localhost';
            let otherMatrixUserId = '@other-user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            await insertUser(context.dbAdapter, otherMatrixUserId, 'cus_124', 'other@example.com');
            let incomingWebhookId = uuidv4();
            let otherIncomingWebhookId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(incomingWebhookId),
                `,`,
                param(matrixUserId),
                `,`,
                param('whk_list1'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(otherIncomingWebhookId),
                `,`,
                param(otherMatrixUserId),
                `,`,
                param('whk_list2'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            await query(context.dbAdapter, [
                `INSERT INTO webhook_commands (id, incoming_webhook_id, command, command_filter, created_at, updated_at) VALUES (`,
                param(uuidv4()),
                `,`,
                param(incomingWebhookId),
                `,`,
                param('https://example.com/handler-1'),
                `,`,
                `'{"eventType": "pull_request"}'::jsonb`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            await query(context.dbAdapter, [
                `INSERT INTO webhook_commands (id, incoming_webhook_id, command, command_filter, created_at, updated_at) VALUES (`,
                param(uuidv4()),
                `,`,
                param(otherIncomingWebhookId),
                `,`,
                param('https://example.com/handler-2'),
                `,`,
                `'{"eventType": "push"}'::jsonb`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let response = await context.request
                .get('/_webhook-commands')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`);
            expect(response.status).toBe(200);
            expect(response.body.data.length).toBe(1);
            expect(response.body.data[0].attributes.incomingWebhookId).toBe(incomingWebhookId);
        });
        it('can filter webhook commands by incomingWebhookId', async function () {
            let matrixUserId = '@user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            let webhookId1 = uuidv4();
            let webhookId2 = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(webhookId1),
                `,`,
                param(matrixUserId),
                `,`,
                param('whk_filter1'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(webhookId2),
                `,`,
                param(matrixUserId),
                `,`,
                param('whk_filter2'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            await query(context.dbAdapter, [
                `INSERT INTO webhook_commands (id, incoming_webhook_id, command, command_filter, created_at, updated_at) VALUES (`,
                param(uuidv4()),
                `,`,
                param(webhookId1),
                `,`,
                param('https://example.com/handler-1'),
                `,`,
                `'{"eventType": "pull_request"}'::jsonb`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            await query(context.dbAdapter, [
                `INSERT INTO webhook_commands (id, incoming_webhook_id, command, command_filter, created_at, updated_at) VALUES (`,
                param(uuidv4()),
                `,`,
                param(webhookId2),
                `,`,
                param('https://example.com/handler-2'),
                `,`,
                `'{"eventType": "push"}'::jsonb`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let response = await context.request
                .get(`/_webhook-commands?incomingWebhookId=${webhookId1}`)
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`);
            expect(response.status).toBe(200);
            expect(response.body.data.length).toBe(1);
            expect(response.body.data[0].attributes.incomingWebhookId).toBe(webhookId1);
        });
        it('can delete own webhook command', async function () {
            let matrixUserId = '@user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            let incomingWebhookId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(incomingWebhookId),
                `,`,
                param(matrixUserId),
                `,`,
                param('whk_del1'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let webhookCommandId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO webhook_commands (id, incoming_webhook_id, command, command_filter, created_at, updated_at) VALUES (`,
                param(webhookCommandId),
                `,`,
                param(incomingWebhookId),
                `,`,
                param('https://example.com/handler'),
                `,`,
                `'{"eventType": "push"}'::jsonb`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let response = await context.request
                .delete('/_webhook-commands')
                .set('Accept', 'application/vnd.api+json')
                .set('Content-Type', 'application/vnd.api+json')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`)
                .send({
                data: {
                    type: 'webhook-command',
                    id: webhookCommandId,
                },
            });
            expect(response.status).toBe(204);
            let rows = await context.dbAdapter.execute(`SELECT id FROM webhook_commands WHERE id = '${webhookCommandId}'`);
            expect(rows.length).toBe(0);
        });
        it('rejects deleting another user webhook command', async function () {
            let matrixUserId = '@user:localhost';
            let otherMatrixUserId = '@other-user:localhost';
            await insertUser(context.dbAdapter, matrixUserId, 'cus_123', 'user@example.com');
            await insertUser(context.dbAdapter, otherMatrixUserId, 'cus_124', 'other@example.com');
            let incomingWebhookId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO incoming_webhooks (id, username, webhook_path, verification_type, verification_config, signing_secret, created_at, updated_at) VALUES (`,
                param(incomingWebhookId),
                `,`,
                param(otherMatrixUserId),
                `,`,
                param('whk_delforbid1'),
                `,`,
                param('HMAC_SHA256_HEADER'),
                `,`,
                `'{"header": "X-Hub-Signature-256", "encoding": "hex"}'::jsonb`,
                `,`,
                param('testsecret'),
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let webhookCommandId = uuidv4();
            await query(context.dbAdapter, [
                `INSERT INTO webhook_commands (id, incoming_webhook_id, command, command_filter, created_at, updated_at) VALUES (`,
                param(webhookCommandId),
                `,`,
                param(incomingWebhookId),
                `,`,
                param('https://example.com/handler'),
                `,`,
                `'{"eventType": "push"}'::jsonb`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `,`,
                `CURRENT_TIMESTAMP`,
                `)`,
            ]);
            let response = await context.request
                .delete('/_webhook-commands')
                .set('Accept', 'application/vnd.api+json')
                .set('Content-Type', 'application/vnd.api+json')
                .set('Authorization', `Bearer ${createRealmServerJWT({ user: matrixUserId, sessionRoom: 'session-room-test' }, realmSecretSeed)}`)
                .send({
                data: {
                    type: 'webhook-command',
                    id: webhookCommandId,
                },
            });
            expect(response.status).toBe(403);
            let rows = await context.dbAdapter.execute(`SELECT id FROM webhook_commands WHERE id = '${webhookCommandId}'`);
            expect(rows.length).toBe(1);
        });
    });
});
