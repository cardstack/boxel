# tests-vitest/ is auto-generated by scripts/codemods/qunit-to-vitest.ts from
# the QUnit suite in tests/. Files graduate to vitest one at a time: each one
# gets allowlisted here and added to vitest.config.ts include[] once it passes.
# Everything else stays out of git until proven to run.
/*
!.gitignore
!async-semaphore.test.ts
