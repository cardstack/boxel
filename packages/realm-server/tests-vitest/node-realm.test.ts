// AUTO-GENERATED by scripts/codemods/qunit-to-vitest.ts. Run codemod again instead of editing by hand.
import {
  describe,
  it,
  expect,
  beforeAll,
  beforeEach,
  afterAll,
  afterEach,
} from 'vitest';
import type { PgAdapter } from '@cardstack/postgres';
import {
  fetchSessionRoom,
  insertPermissions,
  upsertSessionRoom,
} from '@cardstack/runtime-common';
import type { MatrixClient } from '@cardstack/runtime-common/matrix-client';
import type { RealmEventContent } from 'https://cardstack.com/base/matrix-event';
import { NodeAdapter } from '../node-realm';
import { insertUser, setupDB } from './helpers';
describe('node-realm-test.ts', function () {
  const hooks = {
    before: beforeAll,
    after: afterAll,
    beforeEach,
    afterEach,
  };
  let dbAdapter: PgAdapter;
  const realmURL = new URL('http://127.0.0.1:4444/test/');
  const staleRoomId = '!room-alice:localhost';
  setupDB(hooks, {
    beforeEach: async (_dbAdapter) => {
      dbAdapter = _dbAdapter;
    },
  });
  async function insertSessionForAlice() {
    await insertUser(
      dbAdapter,
      '@alice:localhost',
      'cus_alice',
      'alice@example.com',
    );
    await upsertSessionRoom(dbAdapter, '@alice:localhost', staleRoomId);
    await insertPermissions(dbAdapter, realmURL, {
      '@alice:localhost': ['read'],
    });
  }
  function makeEvent(): RealmEventContent {
    return {
      eventName: 'index',
      indexType: 'incremental',
      invalidations: [`${realmURL.href}card`],
      clientRequestId: null,
      realmURL: realmURL.href,
    };
  }
  it('clears a stale session room when the realm server is no longer in it', async function () {
    await insertSessionForAlice();
    let sendEventCalls = 0;
    let matrixClient = {
      login: async () => undefined,
      getUserId: () => '@realm_server:localhost',
      sendEvent: async () => {
        sendEventCalls++;
        throw new Error(
          `Unable to send room event 'app.boxel.realm-event' to room ${staleRoomId}: status 403 - {"errcode":"M_FORBIDDEN","error":"User @realm_server:localhost not in room ${staleRoomId}"}`,
        );
      },
    } as unknown as MatrixClient;
    let adapter = new NodeAdapter('/tmp');
    await adapter.broadcastRealmEvent(
      makeEvent(),
      realmURL.href,
      matrixClient,
      dbAdapter,
    );
    expect(sendEventCalls).toBe(1);
    expect(await fetchSessionRoom(dbAdapter, '@alice:localhost')).toBe(null);
  });
  it('keeps the session room when the send failure is unrelated', async function () {
    await insertSessionForAlice();
    let matrixClient = {
      login: async () => undefined,
      getUserId: () => '@realm_server:localhost',
      sendEvent: async () => {
        throw new Error(
          `Unable to send room event 'app.boxel.realm-event' to room ${staleRoomId}: status 500 - {"errcode":"M_UNKNOWN","error":"boom"}`,
        );
      },
    } as unknown as MatrixClient;
    let adapter = new NodeAdapter('/tmp');
    await adapter.broadcastRealmEvent(
      makeEvent(),
      realmURL.href,
      matrixClient,
      dbAdapter,
    );
    expect(await fetchSessionRoom(dbAdapter, '@alice:localhost')).toBe(
      staleRoomId,
    );
  });
  it('does not reject when clearing a stale session room fails', async function () {
    await insertSessionForAlice();
    let originalExecute = dbAdapter.execute.bind(dbAdapter);
    let failCleanup = false;
    dbAdapter.execute = (async (
      ...args: Parameters<typeof dbAdapter.execute>
    ) => {
      if (failCleanup) {
        throw new Error('boom');
      }
      return await originalExecute(...args);
    }) as typeof dbAdapter.execute;
    let matrixClient = {
      login: async () => undefined,
      getUserId: () => '@realm_server:localhost',
      sendEvent: async () => {
        failCleanup = true;
        throw new Error(
          `Unable to send room event 'app.boxel.realm-event' to room ${staleRoomId}: status 403 - {"errcode":"M_FORBIDDEN","error":"User @realm_server:localhost not in room ${staleRoomId}"}`,
        );
      },
    } as unknown as MatrixClient;
    let adapter = new NodeAdapter('/tmp');
    await adapter.broadcastRealmEvent(
      makeEvent(),
      realmURL.href,
      matrixClient,
      dbAdapter,
    );
    expect(true).toBe(true);
  });
});
