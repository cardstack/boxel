// AUTO-GENERATED by scripts/codemods/qunit-to-vitest.ts. Run codemod again instead of editing by hand.
import {
  describe,
  it,
  expect,
  beforeAll,
  beforeEach,
  afterAll,
  afterEach,
} from 'vitest';
import type { PgAdapter } from '@cardstack/postgres';
import { insertPermissions } from '@cardstack/runtime-common';
import {
  clearSessionRoom,
  fetchSessionRoom,
  fetchRealmSessionRooms,
  upsertSessionRoom,
} from '@cardstack/runtime-common/db-queries/session-room-queries';
import { setupDB, insertUser } from './helpers';
describe('session-room-queries-test.ts', function () {
  describe('fetchRealmSessionRooms', function () {
    const hooks = {
      before: beforeAll,
      after: afterAll,
      beforeEach,
      afterEach,
    };
    let dbAdapter: PgAdapter;
    const realmURL = new URL('http://127.0.0.1:4444/test/');
    setupDB(hooks, {
      beforeEach: async (_dbAdapter) => {
        dbAdapter = _dbAdapter;
      },
    });
    it('returns users with explicit read permission for the realm', async function () {
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      await insertPermissions(dbAdapter, realmURL, {
        '@alice:localhost': ['read'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({
        '@alice:localhost': '!room-alice:localhost',
      });
    });
    it('returns users with explicit write permission for the realm', async function () {
      await insertUser(
        dbAdapter,
        '@bob:localhost',
        'cus_bob',
        'bob@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@bob:localhost',
        '!room-bob:localhost',
      );
      await insertPermissions(dbAdapter, realmURL, {
        '@bob:localhost': ['write'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({
        '@bob:localhost': '!room-bob:localhost',
      });
    });
    it('returns multiple users with different permissions', async function () {
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      await insertUser(
        dbAdapter,
        '@bob:localhost',
        'cus_bob',
        'bob@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@bob:localhost',
        '!room-bob:localhost',
      );
      await insertPermissions(dbAdapter, realmURL, {
        '@alice:localhost': ['read'],
        '@bob:localhost': ['read', 'write'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({
        '@alice:localhost': '!room-alice:localhost',
        '@bob:localhost': '!room-bob:localhost',
      });
    });
    it('excludes users without permission on a non-world-readable realm', async function () {
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      await insertUser(
        dbAdapter,
        '@eve:localhost',
        'cus_eve',
        'eve@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@eve:localhost',
        '!room-eve:localhost',
      );
      // Only alice has permission, not eve
      await insertPermissions(dbAdapter, realmURL, {
        '@alice:localhost': ['read'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({
        '@alice:localhost': '!room-alice:localhost',
      });
    });
    it('excludes users without a session room even when they have permissions', async function () {
      // alice has a session room
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      // bob does NOT have a session room (never authenticated via realm-auth)
      await insertUser(
        dbAdapter,
        '@bob:localhost',
        'cus_bob',
        'bob@example.com',
      );
      await insertPermissions(dbAdapter, realmURL, {
        '@alice:localhost': ['read'],
        '@bob:localhost': ['read'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({
        '@alice:localhost': '!room-alice:localhost',
      });
    });
    it('returns all users with session rooms when the realm is world-readable', async function () {
      // Create multiple users with session rooms
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      await insertUser(
        dbAdapter,
        '@bob:localhost',
        'cus_bob',
        'bob@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@bob:localhost',
        '!room-bob:localhost',
      );
      await insertUser(
        dbAdapter,
        '@charlie:localhost',
        'cus_charlie',
        'charlie@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@charlie:localhost',
        '!room-charlie:localhost',
      );
      // World-readable realm: username='*' with read=true
      // No per-user permission rows for alice, bob, or charlie
      await insertPermissions(dbAdapter, realmURL, {
        '*': ['read'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({
        '@alice:localhost': '!room-alice:localhost',
        '@bob:localhost': '!room-bob:localhost',
        '@charlie:localhost': '!room-charlie:localhost',
      });
    });
    it('world-readable realm still excludes users without session rooms', async function () {
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      // bob has no session room
      await insertUser(
        dbAdapter,
        '@bob:localhost',
        'cus_bob',
        'bob@example.com',
      );
      await insertPermissions(dbAdapter, realmURL, {
        '*': ['read'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({
        '@alice:localhost': '!room-alice:localhost',
      });
    });
    it('returns empty result when no users have permissions', async function () {
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      // No permissions set for this realm at all
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({});
    });
    it('does not return users from a different realm', async function () {
      let otherRealmURL = new URL('http://127.0.0.1:4444/other/');
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      // alice has permission on the OTHER realm, not on our test realm
      await insertPermissions(dbAdapter, otherRealmURL, {
        '@alice:localhost': ['read'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      expect(result).toEqual({});
    });
    it('combines explicit permissions and world-readable access', async function () {
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      await insertUser(
        dbAdapter,
        '@bob:localhost',
        'cus_bob',
        'bob@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@bob:localhost',
        '!room-bob:localhost',
      );
      // alice has explicit write permission AND the realm is world-readable
      await insertPermissions(dbAdapter, realmURL, {
        '*': ['read'],
        '@alice:localhost': ['read', 'write'],
      });
      let result = await fetchRealmSessionRooms(dbAdapter, realmURL.href);
      // Both users should be returned: alice via explicit + world-readable, bob via world-readable
      expect(Object.keys(result).length).toBe(2);
      expect(result['@alice:localhost']).toBe('!room-alice:localhost');
      expect(result['@bob:localhost']).toBe('!room-bob:localhost');
    });
  });
  describe('clearSessionRoom', function () {
    const hooks = {
      before: beforeAll,
      after: afterAll,
      beforeEach,
      afterEach,
    };
    let dbAdapter: PgAdapter;
    setupDB(hooks, {
      beforeEach: async (_dbAdapter) => {
        dbAdapter = _dbAdapter;
      },
    });
    it('clears the stored session room when it matches', async function () {
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      let cleared = await clearSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      expect(cleared).toBe(true);
      let remainingRoom = await fetchSessionRoom(dbAdapter, '@alice:localhost');
      expect(remainingRoom).toBe(null);
    });
    it('does not clear the stored session room when the room id does not match', async function () {
      await insertUser(
        dbAdapter,
        '@alice:localhost',
        'cus_alice',
        'alice@example.com',
      );
      await upsertSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!room-alice:localhost',
      );
      let cleared = await clearSessionRoom(
        dbAdapter,
        '@alice:localhost',
        '!different-room:localhost',
      );
      expect(cleared).toBe(false);
      let remainingRoom = await fetchSessionRoom(dbAdapter, '@alice:localhost');
      expect(remainingRoom).toBe('!room-alice:localhost');
    });
  });
});
