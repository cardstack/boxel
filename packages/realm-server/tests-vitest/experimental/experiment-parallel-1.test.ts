// AUTO-GENERATED by scripts/codemods/qunit-to-vitest.ts. Run codemod again instead of editing by hand.
import { describe, expect } from 'vitest';
import type { RealmAdapter } from '@cardstack/runtime-common';
import {
  type LooseSingleCardDocument,
  SupportedMimeType,
} from '@cardstack/runtime-common';
import {
  createExperimentalPermissionedRealmTest,
  createJWT,
  type ExperimentalPermissionedRealmFixture,
} from '../helpers';
import '@cardstack/runtime-common/helpers/code-equality-assertion';

const realmURL = new URL('http://test-realm/test/');
type ExperimentalRealmTest = {
  concurrent: (
    name: string,
    fn: (context: {
      realm: ExperimentalPermissionedRealmFixture;
    }) => Promise<void>,
  ) => void;
};

const test = createExperimentalPermissionedRealmTest({
  permissions: {
    '*': ['read', 'write'],
    '1': ['read'],
  },
  realmURL,
  serverURL: new URL('http://127.0.0.1:0/test/'),
}) as ExperimentalRealmTest;

describe('atomic-endpoints-test.ts', function () {
  describe('Realm-specific Endpoints: can make request to post /_atomic', function () {
    describe('writes', function () {
      test.concurrent(
        'can write single new module',
        async ({ realm: { request, testRealm, testRealmHref } }) => {
          let source = `
              import { field, CardDef, contains } from "https://cardstack.com/base/card-api";
              import StringField from "https://cardstack.com/base/string";
              export class Place extends CardDef {
                static displayName = 'Place';
                @field name = contains(StringField);
              }
              `.trim();
          let doc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'place-modules/place.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: source,
                  },
                  meta: {},
                },
              },
            ],
          };
          let response = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(doc));
          expect(response.body['atomic:results'].length).toBe(1);
          expect(response.status).toBe(201);
          expect(response.body['atomic:results'][0].data.id).toEqual(
            `${testRealmHref}place-modules/place.gts`,
          );
          let sourceResponse = await request
            .get('/place-modules/place.gts')
            .set('Accept', SupportedMimeType.CardSource);
          expect(sourceResponse.get('X-boxel-realm-url')).toBe(testRealmHref);
          expect(sourceResponse.get('X-boxel-realm-public-readable')).toBe(
            'true',
          );
          expect(sourceResponse.text.trim()).toBe(source);
        },
      );

      test.concurrent(
        'can write multiple new modules',
        async ({ realm: { request, testRealm, testRealmHref } }) => {
          let place1Source = `
              import { field, CardDef, contains } from "https://cardstack.com/base/card-api";
              import StringField from "https://cardstack.com/base/string";
              export class Place extends CardDef {
                static displayName = 'Place';
                @field name = contains(StringField);
              }
              `.trim();
          let place2Source = `
              import { field, CardDef, contains } from "https://cardstack.com/base/card-api";
              import StringField from "https://cardstack.com/base/string";
              export class Place2 extends CardDef {
                static displayName = 'Place2';
                @field name = contains(StringField);
              }
              `.trim();
          let countrySource = `
              import { field, contains } from "https://cardstack.com/base/card-api";
              import StringField from "https://cardstack.com/base/string";
              export class Country extends CardDef {
                static displayName = 'Country';
                @field name = contains(StringField);
              }
              `.trim();
          let doc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'place-modules/place1.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: place1Source,
                  },
                  meta: {},
                },
              },
              {
                op: 'add',
                href: 'place-modules/place2.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: place2Source,
                  },
                },
              },
              {
                op: 'add',
                href: 'country.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: countrySource,
                  },
                  meta: {},
                },
              },
            ],
          };
          let response = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(doc));
          expect(response.status).toBe(201);
          expect(response.body['atomic:results'].length).toBe(3);
          let place1Response = await request
            .get('/place-modules/place1.gts')
            .set('Accept', SupportedMimeType.CardSource);
          expect(place1Response.status).toBe(200);
          expect(place1Response.get('X-boxel-realm-url')).toBe(testRealmHref);
          expect(place1Response.get('X-boxel-realm-public-readable')).toBe(
            'true',
          );
          expect(place1Response.text.trim()).toBe(place1Source);
          let place2Response = await request
            .get('/place-modules/place2.gts')
            .set('Accept', SupportedMimeType.CardSource);
          expect(place2Response.status).toBe(200);
          expect(place2Response.text.trim()).toBe(place2Source);
          expect(place2Response.get('X-boxel-realm-url')).toBe(testRealmHref);
          expect(place2Response.get('X-boxel-realm-public-readable')).toBe(
            'true',
          );
          let countryResponse = await request
            .get('/country.gts')
            .set('Accept', SupportedMimeType.CardSource);
          expect(countryResponse.status).toBe(200);
          expect(countryResponse.text.trim()).toBe(countrySource);
          expect(countryResponse.get('X-boxel-realm-url')).toBe(testRealmHref);
          expect(countryResponse.get('X-boxel-realm-public-readable')).toBe(
            'true',
          );
        },
      );

      test.concurrent(
        'can write a single instance',
        async ({ realm: { request, testRealm } }) => {
          let doc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'new-person-1.json',
                data: {
                  type: 'card',
                  attributes: {
                    firstName: 'Mango',
                  },
                  meta: {
                    adoptsFrom: {
                      module: './person',
                      name: 'Person',
                    },
                  },
                },
              },
            ],
          };
          let response = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(doc));
          expect(response.status).toBe(201);
          expect(response.body['atomic:results'].length).toBe(1);
          let cardResponse = await request
            .get('/new-person-1')
            .set('Accept', SupportedMimeType.CardJson);
          let json = cardResponse.body as LooseSingleCardDocument;
          expect(json.data.attributes?.firstName).toBe('Mango');
        },
      );

      test.concurrent(
        'can write multiple instances',
        async ({ realm: { request, testRealm } }) => {
          let doc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'new-person-1.json',
                data: {
                  type: 'card',
                  attributes: {
                    firstName: 'Mango',
                  },
                  meta: {
                    adoptsFrom: {
                      module: './person',
                      name: 'Person',
                    },
                  },
                },
              },
              {
                op: 'add',
                href: 'new-person-2.json',
                data: {
                  type: 'card',
                  attributes: {
                    firstName: 'Van Gogh',
                  },
                  meta: {
                    adoptsFrom: {
                      module: './person',
                      name: 'Person',
                    },
                  },
                },
              },
            ],
          };
          let response = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(doc));
          expect(response.status).toBe(201);
          expect(response.body['atomic:results'].length).toBe(2);
          let cardResponse1 = await request
            .get('/new-person-1')
            .set('Accept', SupportedMimeType.CardJson);
          let json1 = cardResponse1.body as LooseSingleCardDocument;
          expect(json1.data.attributes?.firstName).toBe('Mango');
          let cardResponse2 = await request
            .get('/new-person-2')
            .set('Accept', SupportedMimeType.CardJson);
          let json2 = cardResponse2.body as LooseSingleCardDocument;
          expect(json2.data.attributes?.firstName).toBe('Van Gogh');
        },
      );

      test.concurrent(
        'can write multiple modules that depend on each other',
        async ({ realm: { request, testRealm, testRealmHref } }) => {
          let place1Source = `
            import { field, CardDef, contains } from "https://cardstack.com/base/card-api";
            import StringField from "https://cardstack.com/base/string";
            export class Place extends CardDef {
              static displayName = 'Place';
              @field name = contains(StringField);
            }
            `.trim();
          let place2Source = `
            import { field, CardDef, contains } from "https://cardstack.com/base/card-api";
            import StringField from "https://cardstack.com/base/string";
            import { Place } from './place'
            export class Place2 extends Place {
              static displayName = 'Place2';
              @field name = contains(StringField);
            }
            `.trim();
          let doc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'place-modules/place1.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: place1Source,
                  },
                  meta: {},
                },
              },
              {
                op: 'add',
                href: 'place-modules/place2.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: place2Source,
                  },
                },
              },
            ],
          };
          let response = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(doc));
          expect(response.status).toBe(201);
          expect(response.body['atomic:results'].length).toBe(2);
          let place1Response = await request
            .get('/place-modules/place1.gts')
            .set('Accept', SupportedMimeType.CardSource);
          expect(place1Response.status).toBe(200);
          expect(place1Response.text.trim()).toBe(place1Source);
          expect(place1Response.get('X-boxel-realm-url')).toBe(testRealmHref);
          expect(place1Response.get('X-boxel-realm-public-readable')).toBe(
            'true',
          );
          let place2Response = await request
            .get('/place-modules/place2.gts')
            .set('Accept', SupportedMimeType.CardSource);
          expect(place2Response.status).toBe(200);
          expect(place2Response.text.trim()).toBe(place2Source);
          expect(place2Response.get('X-boxel-realm-url')).toBe(testRealmHref);
          expect(place2Response.get('X-boxel-realm-public-readable')).toBe(
            'true',
          );
        },
      );

      test.concurrent(
        'can write multiple instances that depend on each other',
        async ({ realm: { request, testRealm } }) => {
          let placeSource = `
              import { field, CardDef, contains, linksTo } from "https://cardstack.com/base/card-api";
              import StringField from "https://cardstack.com/base/string";
              import { Country } from './country'
              export class Place extends CardDef {
                static displayName = 'Place';
                @field name = contains(StringField);
                @field country = linksTo(()=>Country);
              }
              `.trim();
          let countrySource = `
              import { field, CardDef, contains } from "https://cardstack.com/base/card-api";
              import StringField from "https://cardstack.com/base/string";
              export class Country extends CardDef {
                static displayName = 'Country';
                @field name = contains(StringField);
              }
              `.trim();
          let doc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'place.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: placeSource,
                  },
                  meta: {},
                },
              },
              {
                op: 'add',
                href: 'country.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: countrySource,
                  },
                  meta: {},
                },
              },
            ],
          };
          let response = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(doc));
          expect(response.status).toBe(201);
          expect(response.body['atomic:results'].length).toBe(2);
          let placeResponse = await request
            .get('/place.gts')
            .set('Accept', SupportedMimeType.CardSource);
          let countryResponse = await request
            .get('/country.gts')
            .set('Accept', SupportedMimeType.CardSource);
          expect(placeResponse.text.trim()).toBe(placeSource);
          expect(countryResponse.text.trim()).toBe(countrySource);
          let instanceDoc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'malaysia.json',
                data: {
                  type: 'card',
                  attributes: {
                    name: 'Malaysia',
                  },
                  meta: {
                    adoptsFrom: {
                      module: './country',
                      name: 'Country',
                    },
                  },
                },
              },
              {
                op: 'add',
                href: 'menara-kuala-lumpur.json',
                data: {
                  type: 'card',
                  attributes: {
                    name: 'Menara Kuala Lumpur',
                  },
                  relationships: {
                    country: {
                      links: {
                        self: './malaysia.json',
                      },
                      data: {
                        id: '/malaysia.json',
                        type: 'card',
                      },
                    },
                  },
                  meta: {
                    adoptsFrom: {
                      module: './place',
                      name: 'Place',
                    },
                  },
                },
              },
            ],
          };
          let instanceResponse = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(instanceDoc));
          expect(instanceResponse.status).toBe(201);
          expect(instanceResponse.body['atomic:results'].length).toBe(2);
        },
      );

      test.concurrent(
        'can write new instance with new module',
        async ({ realm: { request, testRealm } }) => {
          let source = `
            import { field, CardDef, contains } from "https://cardstack.com/base/card-api";
            import StringField from "https://cardstack.com/base/string";
            export class Place extends CardDef {
              static displayName = 'Place';
              @field name = contains(StringField);
            }
            `.trim();
          let doc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'place-modules/place.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: source,
                  },
                  meta: {},
                },
              },
              {
                op: 'add',
                href: 'place.json',
                data: {
                  type: 'card',
                  attributes: {
                    name: 'Kuala Lumpur',
                  },
                  meta: {
                    adoptsFrom: {
                      module: './place-modules/place.gts',
                      name: 'Place',
                    },
                  },
                },
              },
            ],
          };
          let response = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(doc));
          expect(response.status).toBe(201);
          expect(response.body['atomic:results'].length).toBe(2);
          let cardResponse = await request
            .get('/place')
            .set('Accept', SupportedMimeType.CardJson);
          let json = cardResponse.body as LooseSingleCardDocument;
          expect(json.data.attributes?.name).toBe('Kuala Lumpur');
          let sourceResponse = await request
            .get('/place-modules/place.gts')
            .set('Accept', SupportedMimeType.CardSource);
          expect(sourceResponse.text.trim()).toBe(source);
        },
      );

      test.concurrent(
        'update is a no-op when content is unchanged',
        async ({ realm: { request, testRealm, testRealmAdapter } }) => {
          let source = `
              import { field, CardDef, contains } from "https://cardstack.com/base/card-api";
              import StringField from "https://cardstack.com/base/string";
              export class Place extends CardDef {
                static displayName = 'Place';
                @field name = contains(StringField);
              }
              `.trim();
          let addDoc = {
            'atomic:operations': [
              {
                op: 'add',
                href: 'place-modules/place-noop.gts',
                data: {
                  type: 'source',
                  attributes: {
                    content: source,
                  },
                  meta: {},
                },
              },
            ],
          };
          let addResponse = await request
            .post('/_atomic')
            .set('Accept', SupportedMimeType.JSONAPI)
            .set(
              'Authorization',
              `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
            )
            .send(JSON.stringify(addDoc));
          expect(addResponse.status).toBe(201);
          expect(addResponse.body['atomic:results'].length).toBe(1);
          let initialLastModified = await testRealmAdapter.lastModified(
            'place-modules/place-noop.gts',
          );
          let writeCalls = 0;
          let originalWrite = testRealmAdapter.write.bind(testRealmAdapter);
          testRealmAdapter.write = (async (path, contents) => {
            writeCalls++;
            return originalWrite(path, contents);
          }) as RealmAdapter['write'];
          try {
            let updateDoc = {
              'atomic:operations': [
                {
                  op: 'update',
                  href: 'place-modules/place-noop.gts',
                  data: {
                    type: 'source',
                    attributes: {
                      content: source,
                    },
                    meta: {},
                  },
                },
              ],
            };
            let updateResponse = await request
              .post('/_atomic')
              .set('Accept', SupportedMimeType.JSONAPI)
              .set(
                'Authorization',
                `Bearer ${createJWT(testRealm, 'user', ['read', 'write'])}`,
              )
              .send(JSON.stringify(updateDoc));
            expect(updateResponse.status).toBe(201);
            expect(updateResponse.body['atomic:results'].length).toBe(1);
            expect(writeCalls).toBe(0);
            expect(
              await testRealmAdapter.lastModified(
                'place-modules/place-noop.gts',
              ),
            ).toBe(initialLastModified);
          } finally {
            testRealmAdapter.write = originalWrite;
          }
        },
      );
    });
  });
});
