# File handoff checklist

This small Markdown document verifies that headings, paragraphs, emphasis, and lists render in every Boxel card format.

- Keep the source file beside its card instance.
- Preserve **real file content** during extraction.
- Use the linked `MarkdownDef` as the canonical resource.
