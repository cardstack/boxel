{
  "data": {
    "type": "card",
    "attributes": {
      "cardTitle": "Validate Input",
      "specType": "command",
      "ref": {
        "module": "https://cardstack.com/base/card-api",
        "name": "field"
      },
      "readMe": "# Validate Input\n\n## Summary\nField-level input validation is enforced on write: the `@field` declaration\nis the validation boundary — type coercion, enum membership, and per-field\nrules run when a value is deserialized into the instance.\n\n## Invariants\n- Enum fields reject values outside `options` (see the Enum spec).\n- Polymorphic containers validate each entry against its declared\n  `meta.fields` type; a bare primitive where a FieldDef is expected is a\n  field validation error surfaced at indexing.\n- Validation failures on save surface as indexing errors — the realm\n  serves the error state rather than silently accepting bad data.\n\n## Example\nIndexing rejects `containedExamples: [12.5]` with \"tried set instance of\nArray at index 0 of field 'containedExamples'\"; the entry indexes once it\nis a typed FieldDef value.\n\n## Module\n`https://cardstack.com/base/card-api` — export `field` (decorator);\nenforcement in `field-support.ts`.",
      "containedExamples": [],
      "cardInfo": {
        "name": "Validate Input",
        "summary": null,
        "notes": null,
        "cardThumbnailURL": null
      }
    },
    "meta": {
      "adoptsFrom": {
        "module": "https://cardstack.com/base/spec",
        "name": "Spec"
      }
    }
  }
}
