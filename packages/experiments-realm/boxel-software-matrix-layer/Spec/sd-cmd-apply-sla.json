{
  "data": {
    "type": "card",
    "attributes": {
      "cardTitle": "Apply SLA Policy",
      "cardDescription": "Match a policy to a record and start its clocks.",
      "specType": "command",
      "ref": {
        "module": "../commands/apply-sla-policy-command",
        "name": "ApplySlaPolicyCommand"
      },
      "readMe": "## What it is\nResolves which promise applies, reads its targets, scales them by priority, and converts each into a plain `deadlineAt` using the linked Schedule. **This is the only place business-hours arithmetic runs.**\n\n## Three usages\n1. **ServiceDesk** — run on ticket creation and again whenever priority changes.\n2. **A warranty desk** — applied when a claim is filed against a service tier.\n3. **A delivery promise** — applied at checkout, where the schedule is warehouse shifts.\n\n## The linked example\n`../Ticket/1288-invoice-download` carries the two timers this command produced.\n\n## Non-goals\nNot a scheduler. It computes a deadline once; it cannot wake anything up when that deadline passes — Boxel has no user-defined timed jobs.\n\n**Open question.** It computes a deadline and cannot wake anything up. The missing primitive is a scheduler — filed, not solved.\n\n## Why the example is a linked card\nA command has no renderable instance, so `containedExamples` cannot hold one. The linked card is the command's natural target — open it and the effects described above are visible in its state and its conversation.\n\n## Re-applying is safe\nAn already-satisfied clock keeps its `satisfiedAt`. Re-prioritising a ticket must not erase the fact that we DID answer inside the old target.\n",
      "cardInfo": {
        "name": "Apply SLA Policy"
      }
    },
    "relationships": {
      "linkedExamples.0": {
        "links": {
          "self": "../Ticket/1288-invoice-download"
        }
      }
    },
    "meta": {
      "adoptsFrom": {
        "module": "https://cardstack.com/base/spec",
        "name": "Spec"
      }
    }
  }
}