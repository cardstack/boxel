{
  "data": {
    "type": "card",
    "attributes": {
      "cardTitle": "Resolve Relationship",
      "specType": "command",
      "ref": {
        "module": "https://cardstack.com/base/card-api",
        "name": "linksTo"
      },
      "readMe": "# Resolve Relationship\n\n## Summary\nThe mechanism that turns relationship links into live card instances:\n`linksTo(() => Type)` / `linksToMany(() => Type)` declare the edge; the\nstore loads the target lazily when the field is read.\n\n## Invariants\n- Thunk form (`linksTo(() => X)`) keeps module import cycles inert — two\n  cards may link each other if at least one side is thunked.\n- Reading an unloaded link in a compute can return undefined mid-load;\n  guard computeds.\n- Prerender does not resolve linked images/cards — live queries only need\n  instance rows.\n\n## Example\n```ts\nimport { linksTo, linksToMany } from 'https://cardstack.com/base/card-api';\n\nexport class ConceptReview extends CardDef {\n  // Thunk form keeps a two-way module cycle inert.\n  @field concept = linksTo(() => MatrixConcept);\n  @field reviewer = linksTo(Teammate);\n}\n```\nReading an unloaded link mid-load can return undefined — guard computeds.",
      "containedExamples": [],
      "cardInfo": {
        "name": "Resolve Relationship",
        "summary": null,
        "notes": null,
        "cardThumbnailURL": null
      }
    },
    "meta": {
      "adoptsFrom": {
        "module": "https://cardstack.com/base/spec",
        "name": "Spec"
      }
    }
  }
}