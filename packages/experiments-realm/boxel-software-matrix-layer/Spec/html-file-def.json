{
  "data": {
    "type": "card",
    "attributes": {
      "cardTitle": "HTML File",
      "specType": "card",
      "ref": {
        "module": "https://cardstack.com/base/html-file-def",
        "name": "HtmlDef"
      },
      "readMe": "# HTML File\n\n## Summary\nThe FileDef specialization for HTML documents.\nAn instance is an uploaded realm file with card semantics: the binary lives at\nthe file URL while the card carries extracted metadata and renders with\nformat-aware templates.\n\n## Shape\nExtends `FileDef`. Adds `title`, `excerpt` and `htmlMetadata` (`HtmlMetadataField`), and extracts them on\nupload by parsing the head — title, meta description, Open Graph and charset.\nIts `previewComponent` is the sandboxed HTML preview, which resolves relative\nasset URLs against the file's own realm location.\n\nAccepts: `.html, .htm, text/html`\n\n## Example\n```ts\nimport { HtmlDef } from 'https://cardstack.com/base/html-file-def';\n\n@field attachment = linksTo(HtmlDef);\n```\nInstances are created by upload (`createFileDef` wires the type from the\nMIME type). Live examples need uploaded fixtures — `Spec` has no field that\naccepts a file today (`linkedExamples` is `linksToMany(CardDef)`, and\n`FileDef` is a sibling of `CardDef`), so the example check stays open for\nevery file def until `fileExamples` lands.\n",
      "containedExamples": [],
      "cardInfo": {
        "name": "HTML File",
        "summary": null,
        "notes": null,
        "cardThumbnailURL": null
      }
    },
    "meta": {
      "adoptsFrom": {
        "module": "https://cardstack.com/base/spec",
        "name": "Spec"
      }
    }
  }
}
