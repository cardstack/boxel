<div
  local-class="home-reno-card maximized"
  ...attributes
>
  {{#if @minimize}}
    <button
      local-class="corner-button"
      type="button"
      {{on "click" @minimize}}
    >
      Shrink
    </button>
  {{/if}}
  {{yield}}
</div>