{{#unless this.model.isFastBoot}}
  {{page-title 'Boxel'}}

  <CardCatalog::Modal />
  <CreateCardModal />

  <Editor::Go @openFiles={{this.codeParams}} @monaco={{this.model.monaco}} />

  {{outlet}}
{{/unless}}