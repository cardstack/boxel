{{#unless this.model.isFastBoot}}
  {{page-title 'Boxel'}}

  <CardCatalogModal />
  <CreateCardModal />

  <Editor::Go
    @openFiles={{this.codeParams}}
    @monaco={{this.model.monaco}}
  />

  {{outlet}}
{{/unless}}