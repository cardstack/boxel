{{#unless this.model.isFastBoot}}
  {{page-title 'RuntimeSpike'}}

  <CardCatalogModal />
  <CreateCardModal />

  <Go
    @path={{this.model.path}}
    @openFile={{this.model.openFile}}
    @polling={{this.model.polling}}
  />

  {{outlet}}
{{/unless}}