import Component from '@glimmer/component';

export default class LocalRealmNotConnectedComponent extends Component {
  <template>
    LocalRealmNotConnectedComponent: Local realm not connected.
  </template>
}
