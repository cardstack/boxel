// Server-generated JSON FileDef metadata. This is source identity only, never
// evidence that computations, query membership or code are current.
export interface LatticeJsonSourceFingerprint {
  version: 1;
  algorithm: 'sha256';
  digest: string;
  fileVersion: string;
  contentHash: string;
  contentSize: number;
}
